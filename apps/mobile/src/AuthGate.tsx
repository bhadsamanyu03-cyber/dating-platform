import { useState } from "react";
import { Button, Text, TextInput, View } from "react-native";
import { ProfileScreen } from "./ProfileScreen";
const api = process.env.EXPO_PUBLIC_API_URL ?? "http://localhost:8080/api/v1";
export function AuthGate() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [token, setToken] = useState<string>(); const [error, setError] = useState<string>(); const submit = async (path: "login" | "register") => { setError(undefined); const response = await fetch(`${api}/auth/${path}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, password }) }); if (!response.ok) { const body = await response.json().catch(() => ({})); return setError(body.detail ?? "Unable to continue"); } if (path === "register") return setError("Check your email to verify your account, then sign in."); const body = await response.json(); setToken(body.access_token); }; if (token) return <ProfileScreen accessToken={token} />; return <View style={{ flex: 1, justifyContent: "center", padding: 24, gap: 10 }}><Text>Sign in to manage your profile</Text>{error && <Text style={{ color: "#b00020" }}>{error}</Text>}<TextInput placeholder="Email" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} /><TextInput placeholder="Password" secureTextEntry value={password} onChangeText={setPassword} /><Button title="Sign in" onPress={() => submit("login")} /><Button title="Create account" onPress={() => submit("register")} /></View>; }
